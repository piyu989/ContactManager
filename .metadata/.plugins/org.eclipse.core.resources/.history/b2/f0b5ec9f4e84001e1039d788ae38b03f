<!doctype html>
<html lang="en" xmlns="http://www.thymeleaf.org" th:replace="base::layout(~{::section})">
<head>
	<meta charset="UTF-8" />
	<style>
        bodyy {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        form {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 300px;
        }

        labell {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
        }

        inputt {
            width: 100%;
            padding: 8px;
            margin-bottom: 16px;
            box-sizing: border-box;
        }

        buttonn {
            background-color: #4caf50;
            color: #fff;
            padding: 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            width: 100%;
        }
    </style>
</head>
<bodyy>
	<section>
		<form>
        <labell for="name">Name:</labell>
        <inputt type="text" id="name" name="name" required>

        <labell for="email">Email:</labell>
        <inputt type="email" id="email" name="email" required>

        <labell for="password">Password:</labell>
        <inputt type="password" id="password" name="password" required>

        <labell for="role">Role:</labell>
        <select id="role" name="role">
            <option value="user">User</option>
            <option value="admin">Admin</option>
        </select>

        <labell for="image">Image URL:</labell>
        <inputt type="text" id="image" name="image" placeholder="https://example.com/image.jpg" required>

        <labell for="about">About:</labell>
        <textarea id="about" name="about" rows="4" required></textarea>

        <buttonn type="submit">Sign Up</buttonn>
    </form>
	</section>
</bodyy>
</html> 